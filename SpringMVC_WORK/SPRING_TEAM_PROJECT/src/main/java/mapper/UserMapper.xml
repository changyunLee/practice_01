<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.kr.myapp.service.mapper.UserMapper">

<insert id="join">
	insert into 
	user_cook(user_num,user_id,user_pw,user_name,user_age,user_email)
	values(#{user_num},#{user_id},#{user_pw},#{user_name},#{user_age},#{user_email})	
</insert>

<select id="cntMax" resultType="int">
	select max(user_num)
	from user_cook
</select>

<!-- <select id="idCheck" resultType="co.kr.myapp.model.UserVO"> -->
<!-- 	select * from user_cook -->
<!-- 	where user_id = #{user_id} -->
<!-- </select> -->

</mapper>